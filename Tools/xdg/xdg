#!/usr/bin/env bash
# Wrapper for the SPM xdg audit tool (source of truth: Sources/LambdaTerminalCore/XDGAudit).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$ROOT"
exec swift run xdg "$@"
